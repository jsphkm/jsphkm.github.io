const projectsEl = document.getElementById('projects-ul');

const createEl = (element, class) => {
  const el = document.createElement(element);
  el.className += class;
}

const generateProject = () => {
  
};

const renderProjects = () => {

};

const master = () => {
  renderProjects();
};

window.onload = () => {
  master();
};
